<Application x:Class="Hotel_Una.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Hotel_Una">
    <Application.Resources>
        <!--Button-->
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15, 10"/>
            <Setter Property="Background" Value="#01061B"/>
            <Setter Property="Foreground" Value="#F0F4FF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#001D81"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!--NavButton-->
        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}" x:Key="NavButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!--PrimaryButton-->
        <Style TargetType="Button" x:Key="PrimaryBtn" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#0029B7"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="pack://application:,,,/Hotel_Una;component/Resources/#Poppins Bold"/>
        </Style>
        <!--SecondaryButton-->
        <Style TargetType="Button" x:Key="SecondaryBtn" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#000B31"/>
        </Style>
        <!--Heading-->
        <Style TargetType="TextBlock" x:Key="Heading">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="pack://application:,,,/Hotel_Una;component/Resources/#Poppins Bold"/>
        </Style>
        <!--TextBox-->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="Border"
                        CornerRadius="4"
                        Background="{TemplateBinding Background}"
                        Padding="{TemplateBinding Padding}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">

                            <ScrollViewer x:Name="PART_ContentHost"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Left"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Cursor" Value="IBeam"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!--Colors-->
        <SolidColorBrush x:Key="LightColorBrush">#F0F4FF</SolidColorBrush>
        <SolidColorBrush x:Key="DarkColorBrush">#00061B</SolidColorBrush>
        <SolidColorBrush x:Key="PrimaryColorBrush">#0029B7</SolidColorBrush>
        <SolidColorBrush x:Key="SecondaryColorBrush">#000B31</SolidColorBrush>
        <SolidColorBrush x:Key="AlertColorBrush">#B70000</SolidColorBrush>
        <SolidColorBrush x:Key="WarningColorBrush">#DE7600</SolidColorBrush>
    </Application.Resources>
</Application>
