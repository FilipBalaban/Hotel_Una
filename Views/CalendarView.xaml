<UserControl x:Class="Hotel_Una.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hotel_Una.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Kalendar Rezervacija" Style="{StaticResource Heading}" HorizontalAlignment="Center"/>
        <StackPanel Grid.Row="1" Margin="0, 12, 0, 0" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Margin="0,0,8,0" Command="{Binding LastMonthCommand}" Background="Transparent">
                <fa:ImageAwesome Icon="ChevronLeft" Width="15"/>
            </Button>
            <TextBlock Margin="0,0,8,0" Text="{Binding CurrentMonth}" />
            <Button Command="{Binding NextMonthCommand}" Background="Transparent">
                <fa:ImageAwesome Icon="ChevronRight" Width="15"/>
            </Button>
        </StackPanel>
        <DataGrid Grid.Row="3" Margin="0, 24, 0, 0"/>
    </Grid>
</UserControl>
