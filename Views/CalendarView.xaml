<UserControl x:Class="Hotel_Una.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hotel_Una.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Kalendar Rezervacija" Style="{StaticResource Heading}" HorizontalAlignment="Center"/>
        <StackPanel Grid.Row="1" Margin="0, 12, 0, 0" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Command="{Binding LastMonthCommand}" Background="Transparent">
                <fa:ImageAwesome Icon="ChevronLeft" Width="10"/>
            </Button>
            <TextBlock Margin="8,0,8,0" Text="{Binding CurrentMonth}" FontSize="20" Foreground="{StaticResource DarkColorBrush}" VerticalAlignment="Center" HorizontalAlignment="Center" />
            <Button Command="{Binding NextMonthCommand}" Background="Transparent">
                <fa:ImageAwesome Icon="ChevronRight" Width="10"/>
            </Button>
        </StackPanel>
        <ListView Grid.Row="3" Margin="0, 24, 0, 0" ItemsSource="{Binding Weeks}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="80" Header="Nedelja" DisplayMemberBinding="{Binding Sunday}"/>
                    <GridViewColumn Width="80" Header="Ponedeljak" DisplayMemberBinding="{Binding Monday}"/>
                    <GridViewColumn Width="80" Header="Utorak" DisplayMemberBinding="{Binding Tuesday}"/>
                    <GridViewColumn Width="80" Header="Srijeda" DisplayMemberBinding="{Binding Wednesday}"/>
                    <GridViewColumn Width="80" Header="Četvrtak" DisplayMemberBinding="{Binding Thursday}"/>
                    <GridViewColumn Width="80" Header="Petak" DisplayMemberBinding="{Binding Friday}"/>
                    <GridViewColumn Width="80" Header="Subota" DisplayMemberBinding="{Binding Saturday}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <!--<DataGrid Grid.Row="3" Margin="0, 24, 0, 0" ItemsSource="{Binding Weeks}">
            <DataGrid.Columns>
                <DataGridTextColumn MinWidth="80" Header="Nedelja" Binding="{Binding Sunday}"/>
                <DataGridTextColumn MinWidth="80" Header="Ponedeljak" Binding="{Binding Monday}"/>
                <DataGridTextColumn MinWidth="80" Header="Utorak" Binding="{Binding Tuesday}"/>
                <DataGridTextColumn MinWidth="80" Header="Srijeda" Binding="{Binding Wednesday}"/>
                <DataGridTextColumn MinWidth="80" Header="Četvrtak" Binding="{Binding Thursday}"/>
                <DataGridTextColumn MinWidth="80" Header="Petak" Binding="{Binding Friday}"/>
                <DataGridTextColumn MinWidth="80" Header="Subota" Binding="{Binding Saturday}"/>
            </DataGrid.Columns>
        </DataGrid>-->
    </Grid>
</UserControl>
