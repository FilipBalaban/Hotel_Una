<UserControl x:Class="Hotel_Una.Views.ReservationsOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hotel_Una.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Pregled Rezervacija" Style="{StaticResource Heading}" HorizontalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0, 48, 0, 0">
            <ComboBox Background="{StaticResource PrimaryColorBrush}" Margin="0, 0, 24, 0"/>
            <ComboBox Margin="0, 0, 24, 0"/>
        </StackPanel>
        <ListView Grid.Row="2" Margin="0, 24, 0, 0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="80"/>
                    <GridViewColumn Header="Broj sobe" Width="80"/>
                    <GridViewColumn Header="Ime" Width="80"/>
                    <GridViewColumn Header="Prezime" Width="80"/>
                    <GridViewColumn Header="Datum rezervacije" Width="120"/>
                    <GridViewColumn Header="Check in" Width="80"/>
                    <GridViewColumn Header="Check out" Width="80"/>
                    <GridViewColumn Header="Broj gostiju" Width="80"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
